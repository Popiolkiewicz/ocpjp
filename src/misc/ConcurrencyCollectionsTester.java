package misc;
import java.util.UUID;

public class ConcurrencyCollectionsTester {

	public static void main(String[] args) {
		UUID randomUUID = UUID.randomUUID();
		System.out.println(randomUUID.toString());
	}
}